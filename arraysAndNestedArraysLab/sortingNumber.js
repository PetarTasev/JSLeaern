sortingNumber = (array) => {
    array.sort()
    var half = Math.floor(array.length)
    let firstpart = array
}
sortingNumber ([1, 65, 3, 52, 48, 63, 31, -3, 18, 56])


function sortNumber (array) {
array 
}
